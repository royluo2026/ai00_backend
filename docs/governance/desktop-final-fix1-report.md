# Task11 final fix round1

Scope: the two attachment review findings only. Backend source `5eb7637c063371eefe52f2852082f8c765a8447b`; frontend source `bf9c2d0a108baedc80b398dd4b022f195771b201`. Source and evidence are separate local commits. No delegation, push, merge or publication occurred.

| Finding | Fix and real regression evidence |
| --- | --- |
| P1: caller-writable parent path treated as object ownership | `workmanship_base_historical_uploads` independently records storage key, tenant, object owner/uploader, hash, size, MIME, upload timestamp, exact allowed parent grants and signed creation provenance. Database triggers reject UPDATE/DELETE. The deployment-only importer verifies Ed25519 signatures against a configured trusted public key and active Base super-admin identity; parent records cannot create provenance. Resolver and migration require this row before storage access or reuse of any old binding. Existing typed Artifact upload tables remain for new writes and are not inferred as provenance for unrelated old keys. |
| P2: Task/Issue grid button lacks the actual parent | Actual ListShell callbacks pass the fixed Task/Issue type and row gid to `renderCell`. Delegated preview consumes that separate parent envelope, ignoring attachment or row `parent_ref` overrides. Tenant is absent from caller payload and comes from Gateway identity. The actual GridEditor callback/HTML/button event is tested with JSDOM, production Main client/facade and real Gateway output fixtures. Knowledge and Craft widget click paths are also exercised. |

Both regressions failed before implementation: an owned Knowledge item copied unregistered foreign object bytes into a new Artifact, and the production grid click consumed a forged attachment parent and failed. After the fix, the historical suite has **12 passing tests**, including local/MinIO foreign-key laundering rejection without storage I/O, cross-tenant/owner and wrong-parent rejection, signed importer forgery/conflict rejection, append-only enforcement, valid trusted records, MySQL-normalized JSON replay, and idempotent migration using real SQLite owner queries and ArtifactService.

Final validation: **50 backend tests passed** (two existing Pydantic deprecation warnings); **89 production Gateway executions / 85 IDs and majors** and actual Main/facade projections passed; **78 unpublished candidates** remain closed. Runtime AST scan is **0 literal / 0 dynamic**, 272 files, eight existing auth exceptions, zero parse errors; scanner adversarial tests passed. Full `npm test` passed (Web **154/154**, Agent canvas **14/14**) and production build passed. New real grid/widget clicks pass. Fresh committed frontend source was captured by Electron 41.8.0 with a unique temporary userData directory: **8/8** documented interactions and pixel comparisons passed; seven views have zero changed pixels and workspace has 1,651 (0.07962%). Current approval fixture state transitions also passed. Existing unrelated fixture errors remain in raw logs; shell screenshots do not claim live end-to-end business coverage.

The regenerated 76-group / 115-literal / 43-dynamic matrix has all nine original gap groups closed and `task_complete=true`. Candidate/release `machine_passed`, `human_approved`, `runtime_verified` fields remain false. Production provenance backfill and attachment migration were **not executed**; unbackfilled historical objects fail closed. Upgrade DDL, importer schema/signing procedure and migration commands are documented in [the upgrade procedure](desktop-historical-attachment-upgrade.md).

Provider freeze and committed-bootstrap immutable impact checks pass. The complete Catalog check still reports the same **16 pre-existing Knowledge unbounded collection paths**. No unrelated broad inventory/boundary cleanup was undertaken in this round. Logs are `desktop-final-fix1-{tests,evidence-build,catalog-check,impact}.log`; the paired frontend repository contains scanner, npm, build, projection and fresh UI evidence under the same prefix.
