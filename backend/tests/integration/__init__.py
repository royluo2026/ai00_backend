# Integration test package.
# Static tests (no DB) run with the default offline configuration.
# Tests marked @pytest.mark.integration require AI00_ALLOW_LIVE_DB_TESTS=1 and
# domain-specific DB URL env vars (AI00_CRAFT_DB_URL, etc.).
